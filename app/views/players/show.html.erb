<div class="content">
  <h1><%= @player.name %>'s Teams:</h1>

  <ul>
    <% @player.teams.each do |team| %>
      <li>
      	<%= link_to team.name, team %>
      </li>
    <% end %>
  </ul>

</div>
