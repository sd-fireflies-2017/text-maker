<div class="content">

  <%= form_for @player, method: :patch do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :phone_number_with_area_code %>
    <%= f.text_field :phone_number %>

    <%= f.submit "Update Player" %>

  <% end %>

</div>
